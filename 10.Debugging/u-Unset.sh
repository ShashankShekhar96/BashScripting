#!/bin/bash
clear

# You can also execute this from command line using this command: sh -u filename.sh

val_1=50
val_2=20
sub=$((val_1-val_2))
echo "Answer is $sub"

# When executing using above mentioned command, it will not return any output if the variables are defined correctly. If anything is not correct then an error is returned.
